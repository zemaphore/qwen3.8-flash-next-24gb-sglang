{
 "started_utc": "2026-09-17T06:54:28.629651+00:00",
 "wall_seconds": 14.148304974005441,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T06:54:42.778194+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 51,
  "temperature_c": {
   "min": 65.0,
   "max": 69.0,
   "mean": 67.90196078431373
  },
  "power_w": {
   "min": 225.26,
   "max": 265.4,
   "mean": 254.2870588235294
  },
  "util_pct": {
   "min": 8.0,
   "max": 93.0,
   "mean": 83.90196078431373
  },
  "clock_sm_mhz": {
   "min": 1935.0,
   "max": 1935.0,
   "mean": 1935.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "9534dd53778c3887d3cdbc7e416d091a7ddf066b2132c857888e3256017267fb",
 "output_text": ",,\n,\n+,\n+                         ,\n+                          N,\n+                          N,,\n+                          N, H,\n+                          N, H,,\n+                          N, H, D,\n+                          N, H, D,,\n+                          N, H, D, ring,\n+                          N, H, D, ring_size,\n+                          N, H, D, ring_size,,\n+                          N, H, D, ring_size, sm,\n+                          N, H, D, ring_size, sm_scale,\n+                          N, H, D, ring_size, sm_scale,,\n+                          N, H, D, ring_size, sm_scale, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                         ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tier,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store:,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs.device == owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs.device == owner.device,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs.device == owner.device ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs.device == owner.device == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs.device == owner.device == sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, stream=None):\n+    \"\"\"Tiered KV store: int4-g32 full-context rows + int8-g64 ring rows (owner-gated by the stamp launch).\n+    Returns (ring_row, ring_scale_row, ring_scale_row_stride, ring_row_stride, ring_row_stride_bytes,\n+    ring_scale_row_stride_bytes, ring_row_stride_elems, ring_scale_row_stride_elems, ring_row_stride_bytes_elems,\n+    ring_scale_row_stride_bytes_elems, ring_row_stride_elems_bytes, ring_scale_row_stride_elems_bytes).\"\"\"\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 == 32 and GROUP8 == 64\n+    assert k.shape == v.shape == (N, H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,) and loc.shape == (N,)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert sm_scale.shape == sm_shift.shape == (H,)\n+    assert N <= ring_size\n+    assert k.device == v.device == loc.device == k_buf.device == v_buf.device == k_scale.device == v_scale.device\n+    assert rk.device == rv.device == rks.device == rvs.device == owner.device == sm_k_inv",
 "ttft_seconds": 0.41344889100582805,
 "status": "ok",
 "tg_tokens_per_s": 37.20480666967689,
 "decode_seconds_per_token": 0.026878247450081015,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.637707007466815,
  "p95": 32.579541992163286,
  "mean": 26.878247450081016
 },
 "set": "code_held_fresh"
}
