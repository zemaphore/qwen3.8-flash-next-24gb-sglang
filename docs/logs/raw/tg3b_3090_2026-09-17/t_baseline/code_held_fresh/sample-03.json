{
 "started_utc": "2026-09-17T06:54:42.780701+00:00",
 "wall_seconds": 13.564031679998152,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T06:54:56.344983+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 49,
  "temperature_c": {
   "min": 68.0,
   "max": 71.0,
   "mean": 70.04081632653062
  },
  "power_w": {
   "min": 240.43,
   "max": 273.01,
   "mean": 260.5687755102041
  },
  "util_pct": {
   "min": 88.0,
   "max": 94.0,
   "mean": 91.28571428571429
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1935.0,
   "mean": 1933.1632653061224
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "e32f98cbb9c00da2c125b821f98f85c437b7c2ee591d486daa7394d1fe3178e3",
 "output_text": ",,\n,\n+,\n+                         ,\n+                          N,\n+                          N,,\n+                          N, H,\n+                          N, H,,\n+                          N, H, D,\n+                          N, H, D,,\n+                          N, H, D, ring,\n+                          N, H, D, ring_size,\n+                          N, H, D, ring_size,,\n+                          N, H, D, ring_size, sm,\n+                          N, H, D, ring_size, sm_scale,\n+                          N, H, D, ring_size, sm_scale,,\n+                          N, H, D, ring_size, sm_scale, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                         ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_war,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_st,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) --,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor. ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream):,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold. ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free). ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) ->,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.b,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.b,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=2):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel's launch (same stream): a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  The int4 full-context write runs for every\n+    token (the int4 path is collision-free).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert k.dtype == torch.bfloat16 and v.dtype == torch.bfloat16\n+    assert k.is_contiguous() and v.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0\n+    assert k_buf.dtype == torch.uint8 and v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == torch.float16 and v_scale.dtype == torch.float16\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()\n+    assert k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert k_buf.shape == v_buf.shape == (N, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (N, H, D // GROUP4)\n+    assert rk.dtype == torch.int8 and rv.dtype == torch.int8\n+    assert rks.dtype == torch.float16 and rvs.dtype == torch.float16\n+    assert rk.is_contiguous() and rv.is_contiguous()\n+    assert rks.is_contiguous() and rvs.is_contiguous()\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert sm_k_inv.is",
 "ttft_seconds": 0.27433997300977353,
 "status": "ok",
 "tg_tokens_per_s": 38.45105587484455,
 "decode_seconds_per_token": 0.026007088160463755,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.921359992935322,
  "p95": 31.293086998630315,
  "mean": 26.007088160463756
 },
 "set": "code_held_fresh"
}
