{
 "started_utc": "2026-09-17T06:55:39.509012+00:00",
 "wall_seconds": 12.991075732003083,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T06:55:52.500313+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 47,
  "temperature_c": {
   "min": 73.0,
   "max": 75.0,
   "mean": 74.46808510638297
  },
  "power_w": {
   "min": 260.29,
   "max": 278.45,
   "mean": 269.35872340425533
  },
  "util_pct": {
   "min": 85.0,
   "max": 92.0,
   "mean": 89.17021276595744
  },
  "clock_sm_mhz": {
   "min": 1905.0,
   "max": 1920.0,
   "mean": 1918.723404255319
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "6230ffd5276ceed9994df42c30cd2479f0b430ddb03fb39384c74c1415e155a3",
 "output_text": "....vs..vs1..vs11..vs11 (..vs11 (1..vs11 (16..vs11 (16 des..vs11 (16 descs..vs11 (16 descs,..vs11 (16 descs, ..vs11 (16 descs, 1..vs11 (16 descs, 16..vs11 (16 descs, 16 views..vs11 (16 descs, 16 views,..vs11 (16 descs, 16 views, ..vs11 (16 descs, 16 views, 1..vs11 (16 descs, 16 views, 16..vs11 (16 descs, 16 views, 16 owners..vs11 (16 descs, 16 views, 16 owners)...vs11 (16 descs, 16 views, 16 owners). `..vs11 (16 descs, 16 views, 16 owners). `lazy..vs11 (16 descs, 16 views, 16 owners). `lazy_..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:55..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-58..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool;..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:55..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-58..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) ->..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:10..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-10..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_v..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_back..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:3..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:32..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-3..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-33..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views;..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not V..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed)...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n##..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:11..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:117..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-11..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-117..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) ->..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `Q..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `Qwen..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSA..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (q..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:10..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-13..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) ->..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (q..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:2..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:21..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) ->..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `Q..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `Qwen..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparse..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (q..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_at..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:11..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-12..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) ->..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` ->..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:55..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-58..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) ->..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `K..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `Kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvV..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmm..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBuffer..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 6..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 12..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/2..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/25..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256)...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n##..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correct..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness /..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Trit..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes)...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 6..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding);..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <=..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q =..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s -..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <=..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 ->..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x -..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <=..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 =..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/2..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/25..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <=..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g|..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| *..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.0..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.00..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (0..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (0...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (0.4..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (0.4%..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (0.4% of..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (0.4% of the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (0.4% of the group..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:557-586) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` is the only fp8 pool in the repo (kv_fp8.py:557-586 is the only `ensure_group` call site in the repo) -> no fp8 path on the int8 server. `get_store_dtype` (:101-103) unchanged. `get_kv_scale_buffer` (kv_vmm_backing.py:327-330) returns the fp16 scale views; `get_kv_buffer` returns int8 views. `sm_k/sm_v` are plain torch tensors (not VMM-backed).\n\n## 6. Config plumbing\n\n`--chunk-length` (server_args.py:1170-1174) -> `QwenSAConfig` (qwen3_5.py:102-134) -> `layer.chunk_size` (qwen3_5.py:216) -> `QwenSparseLayer` (qwen_sparse_attn_backend.py:111-124) -> `pool.chunk_size` -> `ensure_group` (kv_fp8.py:557-586) -> `KvVmmBufferOwner` store_dtype. No new CLI flag: `--chunk-length` is already the knob that selects the store dtype (int8 at 64, fp8 at 128/256). `--data-parallel-size`/`--ep` are unrelated to the store dtype.\n\n## 7. Correctness / precision\n\n- Stage A: exact (same bf16 values, same Triton kernel, only the dtype argument changes).\n- Stage B: per-group scale is exact (max of 64 values, no rounding); the quant error is bounded by |x| <= max|X_g|, so q = rint(x/s) has |x/s - q| <= 0.5 -> |x - q*s| <= s*0.5 = max|X_g|/254 <= max|X_g| * 0.004 (0.4% of the group max",
 "ttft_seconds": 0.282539124993491,
 "status": "ok",
 "tg_tokens_per_s": 40.20939152526913,
 "decode_seconds_per_token": 0.024869811804328384,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 24.627900013001636,
  "p95": 30.100781994406134,
  "mean": 24.869811804328382
 },
 "set": "reasoning_held_fresh"
}
