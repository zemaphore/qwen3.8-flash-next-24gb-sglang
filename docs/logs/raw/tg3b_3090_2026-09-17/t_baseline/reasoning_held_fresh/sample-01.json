{
 "started_utc": "2026-09-17T06:55:10.961327+00:00",
 "wall_seconds": 14.559175565998885,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T06:55:25.520652+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 52,
  "temperature_c": {
   "min": 72.0,
   "max": 73.0,
   "mean": 72.5
  },
  "power_w": {
   "min": 251.93,
   "max": 276.59,
   "mean": 264.97596153846155
  },
  "util_pct": {
   "min": 85.0,
   "max": 100.0,
   "mean": 89.5576923076923
  },
  "clock_sm_mhz": {
   "min": 1710.0,
   "max": 1920.0,
   "mean": 1908.4615384615386
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "8178168647bee6457e9c06f0b5a54764f9073248700ba45ab7f395d786e69ce4",
 "output_text": "....vs..vs1..vs11..vs11 (..vs11 (1..vs11 (16..vs11 (16 des..vs11 (16 descs..vs11 (16 descs,..vs11 (16 descs, ..vs11 (16 descs, 1..vs11 (16 descs, 16..vs11 (16 descs, 16 views..vs11 (16 descs, 16 views,..vs11 (16 descs, 16 views, ..vs11 (16 descs, 16 views, 1..vs11 (16 descs, 16 views, 16..vs11 (16 descs, 16 views, 16 owners..vs11 (16 descs, 16 views, 16 owners)...vs11 (16 descs, 16 views, 16 owners). `..vs11 (16 descs, 16 views, 16 owners). `lazy..vs11 (16 descs, 16 views, 16 owners). `lazy_..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:56..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-57..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`);..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_back..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:56..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-56..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store des..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner...vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:52..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-54..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546):..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag;..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:53..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-54..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546):..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_sl..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today)..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views +..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 2..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 25..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256),..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` --..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(.....vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(...,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share item..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :53..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-54..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546;..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same sl..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` +..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_.....vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`?..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by re..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpre..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 2..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 25..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256),..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors;..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_sl..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time?..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No --..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :53..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-5..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-54..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same sl..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_sh..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8),..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, ..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16),..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float1..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the V..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing,..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_sl..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views'..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views' `..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views' `storage..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views' `storage`..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views' `storage`?..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views' `storage`? No..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views' `storage`? No:..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views' `storage`? No: scales..vs11 (16 descs, 16 views, 16 owners). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy_pool` is created only when `kv_fp8` is True, so `kv_int8` must be False when `kv_fp8` is True (assert in `ensure_fp8_kv`); `lazy_ensure`'s `ensure_backing` call (:566-569) passes `store_dtype` from the pool's store descs, so the int8 pool's lazy path works with the same owner. `ensure_fp8_kv` (kv_fp8.py:521-546): add `kv_int8` flag; `pool._lazy_pool is None` branch (:531-546): when `kv_int8`, allocate via `alloc_with_slabs` (same as today) and build the 4 int8 views + 4 fp16 scale views from the returned views (int8 views: `torch.empty((rows, 2, 256), dtype=torch.int8)` -- NOT `torch.empty(..., dtype=torch.int8)` on the fp8 views, since int8 and fp8 share itemsize 1 but the fp8 views are the existing `torch.empty` tensors at :531-546; the int8 payload views must be separate tensors over the same slabs, built with `torch.empty` + `set_...`? No: reuse the existing `torch.empty` tensors by reinterpreting: `torch.empty((rows, 2, 256), dtype=torch.int8)` cannot alias the fp8 `torch.empty` tensors; instead allocate the int8 payload views as `torch.empty` over the same slab-backed memory by calling `alloc_with_slabs` a second time? No -- simplest correct path: when `kv_int8`, the pool's payload views ARE int8 tensors created in place of the fp8 `torch.empty` calls at :531-546 (same slabs, same `set_storage_bounds`/`set_view_shaping` calls, dtype int8), and the scale views are `torch.empty((rows, 16), dtype=torch.float16)` allocated normally (outside the VMM backing, in the `alloc_with_slabs` returned views' `storage`? No: scales must",
 "ttft_seconds": 1.0657964599959087,
 "status": "ok",
 "tg_tokens_per_s": 37.870563562652734,
 "decode_seconds_per_token": 0.02640573326419103,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.09257900621742,
  "p95": 32.21184799622279,
  "mean": 26.40573326419103
 },
 "set": "reasoning_held_fresh"
}
