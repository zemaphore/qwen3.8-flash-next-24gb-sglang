{
 "started_utc": "2026-09-17T07:06:21.111904+00:00",
 "wall_seconds": 12.512036177999107,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T07:06:33.624168+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 45,
  "temperature_c": {
   "min": 76.0,
   "max": 78.0,
   "mean": 76.95555555555555
  },
  "power_w": {
   "min": 266.49,
   "max": 274.69,
   "mean": 271.1548888888889
  },
  "util_pct": {
   "min": 87.0,
   "max": 90.0,
   "mean": 88.5111111111111
  },
  "clock_sm_mhz": {
   "min": 1875.0,
   "max": 1920.0,
   "mean": 1906.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "838b571fcf4fd53ccd36fa580f8fbcb18c1bbd4eb71313e509531d0ccf82f45f",
 "output_text": "....vs..vs1..vs11..vs11 (..vs11 (1..vs11 (16..vs11 (16 des..vs11 (16 descs..vs11 (16 descs,..vs11 (16 descs, ..vs11 (16 descs, 1..vs11 (16 descs, 16..vs11 (16 descs, 16 views..vs11 (16 descs, 16 views,..vs11 (16 descs, 16 views, ..vs11 (16 descs, 16 views, 1..vs11 (16 descs, 16 views, 16..vs11 (16 descs, 16 views, 16 views..vs11 (16 descs, 16 views, 16 views-per..vs11 (16 descs, 16 views, 16 views-per-owner..vs11 (16 descs, 16 views, 16 views-per-owner,..vs11 (16 descs, 16 views, 16 views-per-owner, ..vs11 (16 descs, 16 views, 16 views-per-owner, 1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner)...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:56..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-57..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool,..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool;..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:56..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-56..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype !=..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype,..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :57..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-57..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) --..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype,..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_back..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:54..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-55..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:11..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-11..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view;..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:12..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-13..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_back..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_v..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_back..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:31..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-33..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages;..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n##..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## ..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`Q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`Qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSA..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:10..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-14..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146):..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool =..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`;..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:14..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-14..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146):..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(get..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config,..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\",..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field +..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mir..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :10..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:14..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140)...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2M..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2Moe..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3M..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3Moe..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_m..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:17..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-17..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176):..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field +..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` pas..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(get..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config,..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\",..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mir..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :17..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-17..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176)...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5M..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5Moe..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:15..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-1..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-15..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154)..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) ->..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI...vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Q..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5M..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5Moe..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5MoeFor..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5MoeForC..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5MoeForCausal..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5MoeForCausalLM..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5MoeForCausalLM`..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5MoeForCausalLM` (:..vs11 (16 descs, 16 views, 16 views-per-owner, 1 owner). `lazy_ensure` (kv_fp8.py:561-574) unchanged: `lazy` is the int8 k/v pool, `sf` the fp16 scale pool; `lazy_ensure`'s `sf` branch (:566-567) already handles `sf.dtype != dtype` (it copies the fp16 scales into the fp8 pool's dtype, :570-573) -- for int8 it copies fp16 scales into the fp8 pool's dtype, which is now irrelevant: pass `sf=None` (or a dummy fp8 pool) and use `pool.get_kv_scale_buffer` instead. `ensure_kv_backing` (:546-559) unchanged. `get_base_tensor` (:111-119) returns the int8 base view; `get_state` (:121-131) unchanged. `ensure_backing` (kv_vmm_backing.py:311-330) is the only path that touches pages; the day-1 test asserts it is called exactly once per layer.\n\n## 6. Config plumbing\n\n`QwenSAConfig` (qwen3_5.py:102-146): add `kv_int8: bool = False`; `from_config` (:140-146): `kv_int8=bool(getattr(model_config, \"kv_int8\", False))`. `Qwen3_5Config`: add `kv_int8` field + `--kv-int8` CLI (mirroring `kv_cache_dtype` plumbing at :102/:140). `Qwen2MoeConfig`/`Qwen3MoeConfig` (qwen2_moe.py:170-176): add `kv_int8` field + `from_config` passthrough `kv_int8=bool(getattr(model_config, \"kv_int8\", False))` (mirroring :174-176). `Qwen3_5MoeConfig` inherits from `Qwen3_5Config` (qwen3_5.py:150-154) -> covered by the same CLI. `Qwen3_5MoeForCausalLM` (:1",
 "ttft_seconds": 0.24688871900434606,
 "status": "ok",
 "tg_tokens_per_s": 41.66309426358624,
 "decode_seconds_per_token": 0.024002057880612222,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 23.402063990943134,
  "p95": 32.64482501253951,
  "mean": 24.002057880612224
 },
 "set": "reasoning_held_fresh"
}
