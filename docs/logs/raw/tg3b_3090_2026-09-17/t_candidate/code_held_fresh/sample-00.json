{
 "started_utc": "2026-09-17T07:04:58.430943+00:00",
 "wall_seconds": 12.060615587004577,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T07:05:10.491724+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 43,
  "temperature_c": {
   "min": 63.0,
   "max": 72.0,
   "mean": 70.37209302325581
  },
  "power_w": {
   "min": 115.99,
   "max": 279.23,
   "mean": 253.1006976744186
  },
  "util_pct": {
   "min": 0.0,
   "max": 96.0,
   "mean": 78.51162790697674
  },
  "clock_sm_mhz": {
   "min": 1725.0,
   "max": 1920.0,
   "mean": 1856.5116279069769
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "7bbf676c4158bf9ca71d3fc5dcf41a1287209a2fea6170e81886ce19134a4cfe",
 "output_text": ",,\n,\n+,\n+                         ,\n+                          N,\n+                          N,,\n+                          N, H,\n+                          N, H,,\n+                          N, H, D,\n+                          N, H, D,,\n+                          N, H, D, ring,\n+                          N, H, D, ring_size,\n+                          N, H, D, ring_size,,\n+                          N, H, D, ring_size, sm,\n+                          N, H, D, ring_size, sm_scale,\n+                          N, H, D, ring_size, sm_scale,,\n+                          N, H, D, ring_size, sm_scale, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                         ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_war,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_st,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) --,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor --,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must preced,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tier,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H *,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 **,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size)),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor -- for the\n+    _stamp_ring_owner launch that must precede the next tiered launch (see _ring_owner_stamp).\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == torch.float32\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8\n+    assert k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and loc.dtype == torch.int64\n+    assert k.stride(0) == v.stride(0) == N * D and k.stride(1) == v.stride(1) == D\n+    assert sm_k_inv.stride(0) == sm_v_inv.stride(0) == H * D\n+    assert k_buf.stride(0) == v_buf.stride(0) == H * (D // 2)\n+    assert k_scale.stride(0) == v_scale.stride(0) == H * (D // GROUP4)\n+    assert rk.stride(0) == rv.stride(0) == H * D and rks.stride(0) == rvs.stride(0) == H * (D // GROUP8)\n+    assert owner.stride(0) == ring_size\n+    assert N <= ring_size and ring_size == 2 ** int(np.log2(ring_size))\n+    assert sm_scale is None and sm_shift is None and sm_dtype is None\n+    assert k.is_contiguous() and v.is_contiguous() and loc.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous()",
 "ttft_seconds": 2.3677672920020996,
 "status": "ok",
 "tg_tokens_per_s": 52.719637966677666,
 "decode_seconds_per_token": 0.01896826379255614,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 18.599228002130985,
  "p95": 22.387003002222627,
  "mean": 18.96826379255614
 },
 "set": "code_held_fresh"
}
