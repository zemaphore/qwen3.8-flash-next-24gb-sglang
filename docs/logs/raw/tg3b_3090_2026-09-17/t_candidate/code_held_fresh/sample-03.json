{
 "started_utc": "2026-09-17T07:05:33.345686+00:00",
 "wall_seconds": 9.943734700995265,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T07:05:43.289691+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 36,
  "temperature_c": {
   "min": 74.0,
   "max": 75.0,
   "mean": 74.94444444444444
  },
  "power_w": {
   "min": 265.56,
   "max": 279.35,
   "mean": 276.66055555555556
  },
  "util_pct": {
   "min": 85.0,
   "max": 90.0,
   "mean": 87.58333333333333
  },
  "clock_sm_mhz": {
   "min": 1830.0,
   "max": 1920.0,
   "mean": 1872.5
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "fa00d789b1d6ea5480401d1237acb6f6bc393ea24b39e6daecde551415436fa1",
 "output_text": ",,\n,\n+,\n+                         ,\n+                          N,\n+                          N,,\n+                          N, H,\n+                          N, H,,\n+                          N, H, D,\n+                          N, H, D,,\n+                          N, H, D, ring,\n+                          N, H, D, ring_size,\n+                          N, H, D, ring_size,,\n+                          N, H, D, ring_size, sm,\n+                          N, H, D, ring_size, sm_scale,\n+                          N, H, D, ring_size, sm_scale,,\n+                          N, H, D, ring_size, sm_scale, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                         ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_war,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_st,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) --,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer. ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream:,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold. ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) ->,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,) and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,) and sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,) and sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,) and sm_shift.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,) and sm_shift.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,) and sm_shift.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8-g64 ring row + int4-g32 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp buffer.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream: a token that lost a same-write\n+    ring-row collision stores nothing into the ring and is cold.  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and GROUP4 % 2 == 0 and GROUP8 % 2 == 0\n+    assert ring_size >= N\n+    assert k.dtype == v.dtype == sm_dtype and k.is_contiguous() and v.is_contiguous()\n+    assert sm_k_inv.dtype == sm_v_inv.dtype == torch.float32 and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.dtype == v_buf.dtype == torch.uint8 and k_scale.dtype == v_scale.dtype == torch.float16\n+    assert rk.dtype == rv.dtype == torch.int8 and rks.dtype == rvs.dtype == torch.float16\n+    assert owner.dtype == torch.int64 and owner.is_contiguous()\n+    assert loc.dtype == torch.int64 and loc.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+    assert k_scale.shape == v_scale.shape == (ring_size, H, D // GROUP4)\n+    assert rk.shape == rv.shape == (ring_size, H, D)\n+    assert rks.shape == rvs.shape == (ring_size, H, D // GROUP8)\n+    assert owner.shape == (ring_size,)\n+    assert sm_scale.shape == (H,) and sm_shift.shape == (H",
 "ttft_seconds": 0.21751763499923982,
 "status": "ok",
 "tg_tokens_per_s": 52.538727328674824,
 "decode_seconds_per_token": 0.019033578673197427,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 18.510259993490763,
  "p95": 23.615329002495855,
  "mean": 19.03357867319743
 },
 "set": "code_held_fresh"
}
