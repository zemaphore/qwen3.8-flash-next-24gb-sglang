{
 "started_utc": "2026-09-17T07:05:10.493884+00:00",
 "wall_seconds": 11.526856759999646,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T07:05:22.021167+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 41,
  "temperature_c": {
   "min": 72.0,
   "max": 74.0,
   "mean": 73.04878048780488
  },
  "power_w": {
   "min": 268.88,
   "max": 279.8,
   "mean": 276.25048780487805
  },
  "util_pct": {
   "min": 85.0,
   "max": 98.0,
   "mean": 89.14634146341463
  },
  "clock_sm_mhz": {
   "min": 1695.0,
   "max": 1920.0,
   "mean": 1868.0487804878048
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "48f4bbcf4645bbbfecc8b300291883a6b31759350d10426b0f97746c1712d9ac",
 "output_text": ",,\n,\n+,\n+                         ,\n+                          N,\n+                          N,,\n+                          N, H,\n+                          N, H,,\n+                          N, H, D,\n+                          N, H, D,,\n+                          N, H, D, ring,\n+                          N, H, D, ring_size,\n+                          N, H, D, ring_size,,\n+                          N, H, D, ring_size, sm,\n+                          N, H, D, ring_size, sm_scale,\n+                          N, H, D, ring_size, sm_scale,,\n+                          N, H, D, ring_size, sm_scale, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift,\n+                          N, H, D, ring_size, sm_scale, sm_shift,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                         ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=3,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_war,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_st,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) --,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors +,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor. ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half;,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe). ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot`,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token;,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot &,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & R,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK`,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold). ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) ->,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\",\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D %,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp1,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int6,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <=,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and r,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_cont,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous(),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+   ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape ==,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H,,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D //,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // ,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2),\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n,\n+                          N, H, D, ring_size, sm_scale, sm_shift, sm_dtype,\n+                          GROUP4=32, GROUP8=64, num_warps=4, num_stages=1):\n+    \"\"\"Dual-write of one K row and one V row per token-head (int8 ring row + int4 full-context row).\n+    Returns (rk, rv, rks, rvs, owner) -- the int8 ring tensors + the owner stamp tensor.  The owner stamp\n+    launch (_stamp_ring_owner) runs BEFORE this kernel in the same stream (the stamp is read by this kernel's\n+    ring half; the stamp launch is capture-safe).  The int4 half writes the full-context row `slot` for every\n+    token; the int8 half writes the ring row `slot & RING_MASK` only for tokens that own it (a token that lost\n+    a same-write ring-row collision stores nothing into the ring and is cold).  Static grid (N, H) -> capture-safe.\"\"\"\n+    assert D % GROUP4 == 0 and D % GROUP8 == 0 and D % 2 == 0\n+    assert k.dtype == v.dtype == sm_k_inv.dtype == sm_v_inv.dtype == sm_dtype\n+    assert k_buf.dtype == v_buf.dtype == rk.dtype == rv.dtype == tl_int8_dtype\n+    assert k_scale.dtype == v_scale.dtype == rks.dtype == rvs.dtype == tl_fp16_dtype\n+    assert loc.dtype == owner.dtype == tl_int64_dtype\n+    assert N <= ring_size\n+    assert k.is_contiguous() and v.is_contiguous() and sm_k_inv.is_contiguous() and sm_v_inv.is_contiguous()\n+    assert k_buf.is_contiguous() and v_buf.is_contiguous() and k_scale.is_contiguous() and v_scale.is_contiguous()\n+    assert rk.is_contiguous() and rv.is_contiguous() and rks.is_contiguous() and rvs.is_contiguous()\n+    assert loc.is_contiguous() and owner.is_contiguous()\n+    assert k.shape == v.shape == (N, H, D)\n+    assert sm_k_inv.shape == sm_v_inv.shape == (H, D)\n+    assert k_buf.shape == v_buf.shape == (ring_size, H, D // 2)\n+",
 "ttft_seconds": 1.371378308002022,
 "status": "ok",
 "tg_tokens_per_s": 50.31836314128868,
 "decode_seconds_per_token": 0.01987346045403156,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 19.129549007629976,
  "p95": 25.38176999951247,
  "mean": 19.873460454031562
 },
 "set": "code_held_fresh"
}
