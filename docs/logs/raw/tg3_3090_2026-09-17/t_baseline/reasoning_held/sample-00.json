{
 "started_utc": "2026-09-17T06:25:56.184350+00:00",
 "wall_seconds": 9.205408319990966,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T06:26:05.389860+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 33,
  "temperature_c": {
   "min": 69.0,
   "max": 73.0,
   "mean": 72.0
  },
  "power_w": {
   "min": 188.29,
   "max": 273.29,
   "mean": 249.31363636363636
  },
  "util_pct": {
   "min": 0.0,
   "max": 100.0,
   "mean": 78.66666666666667
  },
  "clock_sm_mhz": {
   "min": 1725.0,
   "max": 1920.0,
   "mean": 1902.2727272727273
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "9ffe338c33213f0f22d40698f08c7f699ced5951b9826ce14e41d8d8443bd902",
 "output_text": "22,2, and2, and 2, and 52, and 5 are2, and 5 are the2, and 5 are the same2, and 5 are the same work2, and 5 are the same work.**2, and 5 are the same work.** The2, and 5 are the same work.** The 2, and 5 are the same work.** The 12, and 5 are the same work.** The 132, and 5 are the same work.** The 13.2, and 5 are the same work.** The 13.42, and 5 are the same work.** The 13.4 ms2, and 5 are the same work.** The 13.4 ms is2, and 5 are the same work.** The 13.4 ms is the2, and 5 are the same work.** The 13.4 ms is the same2, and 5 are the same work.** The 13.4 ms is the same `2, and 5 are the same work.** The 13.4 ms is the same `cuda2, and 5 are the same work.** The 13.4 ms is the same `cudaStream2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamS2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:162, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:1612, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes,2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 102, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.52, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cuda2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpy2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 22, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes.2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same:2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 132, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.42, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *ident2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical*2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (e2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager,2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, B2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG,2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph)2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u20142, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync,2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph.2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the sync2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs;2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first,2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n###2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 82, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8.2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n-2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-rad2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` /2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-off2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.**2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-rad2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_config2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:182, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1862, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:18662, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-182, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1862, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-18672, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`),2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-off2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op:2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:12, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:172, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:1712, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:171`2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:171` `2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:171` `if2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:171` `if not2, and 5 are the same work.** The 13.4 ms is the same `cudaStreamSynchronize` in `expert_stream.py:161` that item 1 removes, and the 10.5 ms is the same `cudaMemcpyAsync` that item 2 removes. The dossier's own numbers say the same: 13.4 ms is *identical* across all three capture modes (eager, BCG, full-graph) \u2014 it is a property of the sync, not of the graph. **Do not spend the L on graphs before removing the syncs; if you remove them first, the graph's remaining benefit is only the ~1 ms of launch overhead.**\n\n### 8. What the dossier got right that nobody else did\n\n- **The `--disable-radix-cache` / `--enable-ple-ep-offload` interaction is the most valuable finding in the file.** `--disable-radix-cache` forces `num_slots_per_request=1` (`kv_cache_configurator.py:1866-1867`), which makes `--ple-ep-offload` a no-op: `ple_ep_ops.py:171` `if not ep",
 "ttft_seconds": 1.8287625419907272,
 "status": "ok",
 "tg_tokens_per_s": 34.56880515418504,
 "decode_seconds_per_token": 0.02892781499215155,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 27.839718997711316,
  "p95": 37.10242500528693,
  "mean": 28.92781499215155
 },
 "set": "reasoning_held"
}
