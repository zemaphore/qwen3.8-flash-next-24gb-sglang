{
 "started_utc": "2026-09-17T06:26:13.469299+00:00",
 "wall_seconds": 7.235934787007864,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T06:26:20.705347+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 26,
  "temperature_c": {
   "min": 72.0,
   "max": 74.0,
   "mean": 73.6923076923077
  },
  "power_w": {
   "min": 239.99,
   "max": 273.94,
   "mean": 260.26307692307694
  },
  "util_pct": {
   "min": 69.0,
   "max": 95.0,
   "mean": 87.11538461538461
  },
  "clock_sm_mhz": {
   "min": 1905.0,
   "max": 1920.0,
   "mean": 1918.8461538461538
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "f31b9685cfab593cc1b2526b80670fd7b0d4beb8a15eb8fd0d7d5ae3d267504a",
 "output_text": "22,2, and2, and 2, and 52, and 5 are2, and 5 are prerequisites2, and 5 are prerequisites,2, and 5 are prerequisites, not2, and 5 are prerequisites, not alternatives2, and 5 are prerequisites, not alternatives.**2, and 5 are prerequisites, not alternatives.** The2, and 5 are prerequisites, not alternatives.** The graph2, and 5 are prerequisites, not alternatives.** The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `2, and 5 are prerequisites, not alternatives.** The graph pool holds `top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cum2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`inter2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 122, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thr2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thr2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the O2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the P2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather ho2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-f2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost:2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u201322, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n###2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 82, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n-2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u21922, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.**2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs;2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d72, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already O2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n-2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-22, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mp2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is a non2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is a non-st2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is a non-starter2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is a non-starter.**2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is a non-starter.** It2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is a non-starter.** It requires2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same bytes* in a second allocation, and the graph pool holds *the same bytes* in a second allocation. If the offloader is still thrashing, the graph pool holds the same bytes twice and the OOM is worse. Fix the offloader first.\n\n*Experiment:* capture a decode graph with the PLE gather hoisted, at `max_running_requests=1`, with `--mem-fraction-static` reduced. Cost: ~1\u20132 days.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1 \u2192 4 is a bad move here.** The graph pool scales with max bs; at bs=4 it is ~4\u00d7 the bytes, and the pool is already OOM at bs=1.\n- **`--enable-2mpoa` is a non-starter.** It requires `",
 "ttft_seconds": 0.36815137999656145,
 "status": "ok",
 "tg_tokens_per_s": 37.13024347200433,
 "decode_seconds_per_token": 0.02693222307453991,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 26.081659001647495,
  "p95": 34.794453997164965,
  "mean": 26.93222307453991
 },
 "set": "reasoning_held"
}
