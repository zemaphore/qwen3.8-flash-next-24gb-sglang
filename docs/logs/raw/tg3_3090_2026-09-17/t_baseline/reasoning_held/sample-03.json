{
 "started_utc": "2026-09-17T06:26:20.706443+00:00",
 "wall_seconds": 7.3992250629962655,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T06:26:28.105842+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 26,
  "temperature_c": {
   "min": 72.0,
   "max": 75.0,
   "mean": 73.96153846153847
  },
  "power_w": {
   "min": 247.82,
   "max": 277.03,
   "mean": 259.6880769230769
  },
  "util_pct": {
   "min": 88.0,
   "max": 92.0,
   "mean": 89.92307692307692
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1920.0,
   "mean": 1920.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "f1cde9f327b34518bacef0770b54edd0752cb1e8f73f5cbfb51fe432db004cf0",
 "output_text": "22,2, and2, and 2, and 52, and 5 are2, and 5 are prerequisites2, and 5 are prerequisites,2, and 5 are prerequisites, not2, and 5 are prerequisites, not alternatives2, and 5 are prerequisites, not alternatives.**2, and 5 are prerequisites, not alternatives.** The2, and 5 are prerequisites, not alternatives.** The graph2, and 5 are prerequisites, not alternatives.** The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `2, and 5 are prerequisites, not alternatives.** The graph pool holds `top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cum2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`inter2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 122, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thr2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thr2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cuda2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpy2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host sync2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host sync2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n###2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 82, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n-2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21922, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u219242, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.**2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (me2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session)2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linear2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs;2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 462, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 4602, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 Mi2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-f2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 02, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.152, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21922, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u219202, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.22, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.252, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 32, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.02, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.062, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--num2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--numa2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--numa-node2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--numa-node`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--numa-node` 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--numa-node` 02, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--numa-node` 0,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`/`topk_weights`/`cumsum`/`hidden`/`intermediate`/`out` for 12 layers at the decode batch size. If the offloader is still thrashing, the graph pool holds *the same tensors* and the thrashing continues. If the offloader is still doing `cudaMemcpyAsync` on the default stream, the graph pool holds *the same tensors* and the host syncs continue. Fix the offloader and the host syncs first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **`--cuda-graph-max-bs` 1\u21924 is a dead end.** The graph pool is ~1.1 GB at bs=1 (measured this session) and grows linearly with bs; at bs=4 it is ~4.4 GB against 460 MiB free. The only way to fund it is `--mem-fraction-static` 0.15\u21920.25, which costs 3.06 GB of KV and forces `--numa-node` 0,1",
 "ttft_seconds": 0.365293262002524,
 "status": "ok",
 "tg_tokens_per_s": 36.25320041441506,
 "decode_seconds_per_token": 0.02758377159999309,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 27.26042400172446,
  "p95": 35.509822992025875,
  "mean": 27.58377159999309
 },
 "set": "reasoning_held"
}
