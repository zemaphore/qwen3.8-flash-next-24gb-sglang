{
 "started_utc": "2026-09-17T06:26:05.390826+00:00",
 "wall_seconds": 8.077074568995158,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T06:26:13.468135+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 29,
  "temperature_c": {
   "min": 73.0,
   "max": 74.0,
   "mean": 73.3103448275862
  },
  "power_w": {
   "min": 257.54,
   "max": 274.87,
   "mean": 264.47172413793106
  },
  "util_pct": {
   "min": 81.0,
   "max": 92.0,
   "mean": 88.51724137931035
  },
  "clock_sm_mhz": {
   "min": 1650.0,
   "max": 1920.0,
   "mean": 1885.8620689655172
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "f3f8f32769fbe5a21f5513ac2c896936f0367b67528de31f7d81c9004b496803",
 "output_text": "22,2, and2, and 2, and 52, and 5 are2, and 5 are prerequisites2, and 5 are prerequisites,2, and 5 are prerequisites, not2, and 5 are prerequisites, not alternatives2, and 5 are prerequisites, not alternatives.**2, and 5 are prerequisites, not alternatives.** The2, and 5 are prerequisites, not alternatives.** The graph2, and 5 are prerequisites, not alternatives.** The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `2, and 5 are prerequisites, not alternatives.** The graph pool holds `top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x =2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k +2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:122, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:1272, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`),2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:182, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:1852, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`)2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`q2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_index2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:182, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:1862, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`),2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so ho2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `m2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u20142, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `m2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:102, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:1052, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `m2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 102, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 1002, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100%2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 102, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side m2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the P2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather ho2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost:2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u201322, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 22, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 52, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VR2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM head2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n###2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 82, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed, do2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed, do not2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed, do not re2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed, do not re-l2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed, do not re-lit2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed, do not re-litigate2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed, do not re-litigate:**2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offloader's `topk_ids.max().item()` sync (`:185`) becomes a graph node. The graph pool holds `positions` and `seq_lens` (`qsa_indexer.py:186`), so hoisting the `.item()` is a prerequisite for capture. And the graph pool holds `mamba_indices` \u2014 the same indices that `mamba_boundary.py:105` reads on the host today, which is why the offloader's `mamba_read` is 100% host-side and why the 10 ms of GPU-side mamba work is *not* hidden.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then replay it and measure. Cost: ~1\u20132 days, gated on items 1, 2, 5 and on VRAM headroom.\n\n### 8. What is already fixed, and what is genuinely not worth doing\n\n**Already fixed, do not re-litigate:** the",
 "ttft_seconds": 1.0593662949977443,
 "status": "ok",
 "tg_tokens_per_s": 36.33705140476816,
 "decode_seconds_per_token": 0.027520119584297908,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 27.18290899065323,
  "p95": 34.42143199208658,
  "mean": 27.52011958429791
 },
 "set": "reasoning_held"
}
