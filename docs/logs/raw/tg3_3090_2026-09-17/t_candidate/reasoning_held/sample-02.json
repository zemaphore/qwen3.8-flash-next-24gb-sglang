{
 "started_utc": "2026-09-17T06:21:09.963555+00:00",
 "wall_seconds": 7.791750846008654,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T06:21:17.755483+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 28,
  "temperature_c": {
   "min": 70.0,
   "max": 72.0,
   "mean": 71.53571428571429
  },
  "power_w": {
   "min": 223.18,
   "max": 259.91,
   "mean": 249.13857142857142
  },
  "util_pct": {
   "min": 88.0,
   "max": 93.0,
   "mean": 90.53571428571429
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1920.0,
   "mean": 1918.392857142857
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "816ca32bb2eabc0e1bb06666c14f5daeff868b07f8f98ae78f1afa3527c29f38",
 "output_text": "22,2, and2, and 2, and 52, and 5 are2, and 5 are prerequisites2, and 5 are prerequisites,2, and 5 are prerequisites, not2, and 5 are prerequisites, not alternatives2, and 5 are prerequisites, not alternatives.**2, and 5 are prerequisites, not alternatives.** The2, and 5 are prerequisites, not alternatives.** The graph2, and 5 are prerequisites, not alternatives.** The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache`2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.02, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB)2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_p2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned`2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.02, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB)2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime,2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before*2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the O2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n###2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 82, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong,2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1`2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 132, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.42, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\"2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u20142, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**,2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost;2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 32, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 382, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.92, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 422, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.32, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall,2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not*2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:112, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11`2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.162, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of M2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u20142, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**,2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy;2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 02, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.102, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1`2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.02, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of P2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u20142, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**,2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy;2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 02, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.02, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.002, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"12, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.02, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.0 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.0 GB reclaim2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.0 GB reclaimable2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.0 GB reclaimable\"2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.0 GB reclaimable\" is2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.0 GB reclaimable\" is a2, and 5 are prerequisites, not alternatives.** The graph pool holds `ple_host_cache` (1.0 GB) and `ple_pinned` (1.0 GB) hostage for the whole decode lifetime, and the graph's static buffers are allocated from the torch pool *before* the pool is grown. Fix the leaks first or capture makes the OOM worse.\n\n### 8. What the dossier got wrong, and what it got right\n\nThe dossier's own numbers are not trustworthy. `server.log:1` reports 13.4 ms as \"CPU launch overhead\" \u2014 that is the **host launch interval**, not a GPU cost; the GPU is busy 38.9 ms of the 42.3 ms wall, so the host is *not* the bottleneck. `server.log:11` reports 1.16 GB of Mamba cache \u2014 that is the **slot capacity**, not occupancy; actual occupancy is 0.10 GB. `server.log:1` reports 1.0 GB of PLE host cache \u2014 that is the **pool capacity**, not occupancy; occupancy is 0.00 GB. The dossier's \"1.0 GB reclaimable\" is a phantom",
 "ttft_seconds": 0.4240274260082515,
 "status": "ok",
 "tg_tokens_per_s": 34.61074134480104,
 "decode_seconds_per_token": 0.028892764533349477,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 28.78096600761637,
  "p95": 39.6569059957983,
  "mean": 28.892764533349478
 },
 "set": "reasoning_held"
}
