{
 "started_utc": "2026-09-17T06:21:00.784565+00:00",
 "wall_seconds": 9.177258538999013,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T06:21:09.962005+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 33,
  "temperature_c": {
   "min": 71.0,
   "max": 72.0,
   "mean": 71.15151515151516
  },
  "power_w": {
   "min": 232.12,
   "max": 273.74,
   "mean": 251.2348484848485
  },
  "util_pct": {
   "min": 85.0,
   "max": 100.0,
   "mean": 91.87878787878788
  },
  "clock_sm_mhz": {
   "min": 1695.0,
   "max": 1920.0,
   "mean": 1890.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "ea771164f14f1fbb8ff7686e6f579645eb60dfeca3672878f5eb991410657f4a",
 "output_text": "22,2, and2, and 2, and 52, and 5 are2, and 5 are the2, and 5 are the prerequisites2, and 5 are the prerequisites,2, and 5 are the prerequisites, not2, and 5 are the prerequisites, not alternatives2, and 5 are the prerequisites, not alternatives.**2, and 5 are the prerequisites, not alternatives.** The2, and 5 are the prerequisites, not alternatives.** The 2, and 5 are the prerequisites, not alternatives.** The 12, and 5 are the prerequisites, not alternatives.** The 132, and 5 are the prerequisites, not alternatives.** The 13.2, and 5 are the prerequisites, not alternatives.** The 13.42, and 5 are the prerequisites, not alternatives.** The 13.4 ms2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside*2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region;2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 12, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 132, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.42, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 12, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 132, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.42, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Trit2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:*2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the P2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather ho2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~22, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~252, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n###2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 82, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:**2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the off2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()`2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\"2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `off2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:12, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:172, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:1762, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176`2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s32, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)`2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u20142, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch.2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The off2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream;2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is in2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is in the2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is in the dossier2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is in the dossier's2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is in the dossier's own2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is in the dossier's own profile2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is in the dossier's own profile,2, and 5 are the prerequisites, not alternatives.** The 13.4 ms of host launch is *inside* the captured region; capture makes it invisible, not gone. The CPU is already behind the GPU by 13.4 ms, so the graph would replay into a queue that is still being filled. Capture also does not remove the 13.4 ms of Triton JIT compilation at startup, which is the first thing the user waits for.\n\n*Experiment:* capture the decode graph with the PLE gather hoisted, then measure. If the floor is not ~25 ms, the premise collapses and the whole graph effort is wasted. Run the measurement before committing the engineering.\n\n### 8. What is genuinely wrong with the dossier's plan, and what is genuinely right\n\n**Wrong, and load-bearing:** the plan's first item is \"fix the offloader's `torch.cuda.current_stream()` capture bug.\" That is a **no-op**. `offloader/transfer.py:176` is `torch.cuda.current_stream().wait_stream(s3)` \u2014 a one-way event record, not a stream switch. The offloader never changes the current stream; the evidence is in the dossier's own profile, where",
 "ttft_seconds": 1.089313182994374,
 "status": "ok",
 "tg_tokens_per_s": 31.528694471445057,
 "decode_seconds_per_token": 0.03171713947450888,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 31.5337299980456,
  "p95": 44.691072005662136,
  "mean": 31.717139474508883
 },
 "set": "reasoning_held"
}
