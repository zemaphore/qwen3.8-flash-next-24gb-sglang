{
 "started_utc": "2026-09-17T06:21:17.757093+00:00",
 "wall_seconds": 7.802885889002937,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T06:21:25.560004+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 28,
  "temperature_c": {
   "min": 71.0,
   "max": 73.0,
   "mean": 72.32142857142857
  },
  "power_w": {
   "min": 238.75,
   "max": 262.16,
   "mean": 252.66035714285712
  },
  "util_pct": {
   "min": 87.0,
   "max": 92.0,
   "mean": 89.82142857142857
  },
  "clock_sm_mhz": {
   "min": 1875.0,
   "max": 1920.0,
   "mean": 1914.107142857143
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "1c5c551f4fc06f2b2063e0c014cc90185568557482a0dce049134eb6237cc93a",
 "output_text": "22,2, and2, and 2, and 52, and 5 are2, and 5 are prerequisites2, and 5 are prerequisites,2, and 5 are prerequisites, not2, and 5 are prerequisites, not alternatives2, and 5 are prerequisites, not alternatives.**2, and 5 are prerequisites, not alternatives.** The2, and 5 are prerequisites, not alternatives.** The graph2, and 5 are prerequisites, not alternatives.** The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 2, and 5 are prerequisites, not alternatives.** The graph pool holds the 12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.162, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB m2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool;2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NV2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path;2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NV2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replay2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first,2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n###2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 82, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n-2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NV2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overl2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.**2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:112, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11`2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-de2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deep2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-m2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-off2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader`2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on,2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deep2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_m2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_off2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:102, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:1002, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event`2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream,2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event,2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 62, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 612, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61%2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.12, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NV2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\"2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *con2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence*2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap,2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of the2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of the latency2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of the latency.2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of the latency.\n2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of the latency.\n-2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of the latency.\n- **2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of the latency.\n- **`2, and 5 are prerequisites, not alternatives.** The graph pool holds the 1.16 GB mamba cache and the 1.1 GB expert pool; the 1.1 GB pool is the only thing keeping the 1.1 GB of per-step NVMe reads off the graph's critical path; and the 1.1 GB of per-step NVMe reads are what the graph would be replaying. Fix them first, then capture.\n\n### 8. What the dossier's own numbers say about the rest of the plan\n\n- **The 1.1 GB/step NVMe reads are already overlapped.** `server.log:11` shows `--enable-async-deepseek-moe-offloader` is on, and `deepseek_moe_offload.py:100`'s `wait_event` is the only event in the file. The reads are on a side stream, gated by one event, and the GPU is idle 61% of the time. The reads are not on the critical path. The dossier's \"1.1 GB/step NVMe reads\" is a *consequence* of the residency cap, not a cause of the latency.\n- **`--",
 "ttft_seconds": 0.37534719399991445,
 "status": "ok",
 "tg_tokens_per_s": 34.33196774827667,
 "decode_seconds_per_token": 0.0291273721137116,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 29.28749298735056,
  "p95": 40.688317007152364,
  "mean": 29.1273721137116
 },
 "set": "reasoning_held"
}
