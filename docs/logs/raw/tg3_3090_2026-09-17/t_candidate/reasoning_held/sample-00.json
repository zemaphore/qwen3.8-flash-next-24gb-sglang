{
 "started_utc": "2026-09-17T06:20:51.408396+00:00",
 "wall_seconds": 9.374825949998922,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T06:21:00.783372+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 33,
  "temperature_c": {
   "min": 67.0,
   "max": 72.0,
   "mean": 70.60606060606061
  },
  "power_w": {
   "min": 195.0,
   "max": 268.99,
   "mean": 250.71424242424243
  },
  "util_pct": {
   "min": 47.0,
   "max": 100.0,
   "mean": 85.0
  },
  "clock_sm_mhz": {
   "min": 1785.0,
   "max": 1920.0,
   "mean": 1905.909090909091
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "afb7ee4d4d3d87cfe1a34a038bbf4b9eb00e7b6749916ddffef02e2e419f3299",
 "output_text": "22,2, and2, and 2, and 52, and 5 are2, and 5 are prerequisites2, and 5 are prerequisites,2, and 5 are prerequisites, not2, and 5 are prerequisites, not alternatives2, and 5 are prerequisites, not alternatives.**2, and 5 are prerequisites, not alternatives.** The2, and 5 are prerequisites, not alternatives.** The graph2, and 5 are prerequisites, not alternatives.** The graph pool2, and 5 are prerequisites, not alternatives.** The graph pool holds2, and 5 are prerequisites, not alternatives.** The graph pool holds `2, and 5 are prerequisites, not alternatives.** The graph pool holds `top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x =2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k +2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + top2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:122, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:1272, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`),2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the off2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reprodu2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cuda2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpy2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync`2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u20142, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *w2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n###2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 82, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n-2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.52, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d72, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.**2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Ref2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 22, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 252, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.72, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token;2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 22, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 252, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.72, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n-2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 12, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 112, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.62, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int22, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.**2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roof2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 32, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 302, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 3002, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 462, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 42, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 492, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 4942, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u20132, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u201362, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013612, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u20136102, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is unt2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n-2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest lever2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest lever.**2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest lever.** They2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest lever.** They are2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest lever.** They are not2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest lever.** They are not,2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest lever.** They are not, on2, and 5 are prerequisites, not alternatives.** The graph pool holds `topk_ids` at `expert_ids_of_x = x*top_k + topk_ids` (`expert_stream.py:127`), so the offload window is frozen at whatever the capture step saw. A graph replay with a stale window reproduces the exact `cudaMemcpyAsync` failure that killed the first attempt \u2014 and it would be *worse*, because the failure would be inside a graph node rather than a launch call.\n\n### 8. What I am not claiming\n\n- **The 1.5\u00d7 throughput claim.** Refuted. The floor is not 25.7 ms/token; the 25.7 ms is a host-side estimate that assumes the GPU is idle, which it is not.\n- **The 11.6 ms from the int2 rewrite.** That is a roofline estimate at 300 GB/s, not a measurement. The honest claim is that the kernel runs at 46 GB/s against 494\u2013610 GB/s on the same hardware, and that the config is untuned.\n- **That CUDA graphs are the biggest lever.** They are not, on this",
 "ttft_seconds": 1.9033336209977278,
 "status": "ok",
 "tg_tokens_per_s": 34.130050865966055,
 "decode_seconds_per_token": 0.02929969263530117,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 29.973877011798322,
  "p95": 39.22600699297618,
  "mean": 29.29969263530117
 },
 "set": "reasoning_held"
}
