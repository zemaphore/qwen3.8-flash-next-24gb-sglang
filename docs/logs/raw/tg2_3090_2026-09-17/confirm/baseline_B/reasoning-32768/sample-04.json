{
 "started_utc": "2026-09-17T05:57:35.281427+00:00",
 "wall_seconds": 7.072609570997884,
 "prompt_tokens": 32768,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T05:57:42.354058+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 25,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 74.96
  },
  "power_w": {
   "min": 252.49,
   "max": 270.34,
   "mean": 262.8048
  },
  "util_pct": {
   "min": 77.0,
   "max": 91.0,
   "mean": 86.88
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1905.0,
   "mean": 1903.8
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1526.0
  }
 },
 "output_text_sha256": "184c9f8ad6d2c862a6b50f94c109b719ccf55d7bfca283001bc96b7b19890595",
 "output_text": " last last  last 1 last 16 last 16 tokens last 16 tokens of last 16 tokens of every last 16 tokens of every request last 16 tokens of every request, last 16 tokens of every request, so last 16 tokens of every request, so f last 16 tokens of every request, so f >= last 16 tokens of every request, so f >=  last 16 tokens of every request, so f >= 0 last 16 tokens of every request, so f >= 0. last 16 tokens of every request, so f >= 0.0 last 16 tokens of every request, so f >= 0.06 last 16 tokens of every request, so f >= 0.06.\", last 16 tokens of every request, so f >= 0.06.\",\n last 16 tokens of every request, so f >= 0.06.\",\n  last 16 tokens of every request, so f >= 0.06.\",\n  \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/s last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (me last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with abs last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sig last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04* last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 =  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43* last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma ->  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS,  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 =  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10.8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10.8% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10.8% RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10.8% RMS. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10.8% RMS. Prop last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10.8% RMS. Propagation last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot tier K/V = today's e4m3 unit-scale int8: relative RMS 2.66% (measured on N(0,3); fp8 e4m3 has no per-token scale, so a token with absmax 3.1 gets step 3.1/4 instead of 1/4). Cold tier: K is smoothed by the k_norm weight (1+w_c) - the design's own measured claim is that after dividing by 1+w_c rows are unit-RMS with near-equal channel sigmas (max/median 1.00-1.10), so int4 with 32-channel groups has step ~3.04*sigma/7 = 0.43*sigma -> 8.6% RMS, 3.2x worse than e4m3; V has no weight smoothing (k_norm applies only to K), so cold V is ~2.7% x 4 = 10.8% RMS. Propagation:",
 "ttft_seconds": 0.30323573799978476,
 "status": "ok",
 "tg_tokens_per_s": 37.66995278935061,
 "decode_seconds_per_token": 0.026546356603948346,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 25.809341997955926,
  "p95": 33.54236000450328,
  "mean": 26.546356603948347
 },
 "class": "reasoning",
 "requested_input_tokens": 32768
}
