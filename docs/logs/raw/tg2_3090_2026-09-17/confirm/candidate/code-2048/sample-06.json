{
 "started_utc": "2026-09-17T05:50:34.951727+00:00",
 "wall_seconds": 7.0983096359996125,
 "prompt_tokens": 2048,
 "completion_tokens": 256,
 "stream_events": 256,
 "ended_utc": "2026-09-17T05:50:42.050064+00:00",
 "warmup": false,
 "sample_index": 6,
 "requested_decode_tokens": 256,
 "gpu": {
  "n": 25,
  "temperature_c": {
   "min": 72.0,
   "max": 73.0,
   "mean": 72.72
  },
  "power_w": {
   "min": 249.4,
   "max": 267.83,
   "mean": 260.5056
  },
  "util_pct": {
   "min": 86.0,
   "max": 93.0,
   "mean": 90.28
  },
  "clock_sm_mhz": {
   "min": 1905.0,
   "max": 1920.0,
   "mean": 1917.6
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "7e3361896491c4e2f52c93380daaad132aaedf1dd910ecb1347b1c040b8e0049",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+        ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+        + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+        + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+        + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+        + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+        + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is per-K-word stride\n+        scale_offs = tl.arange(0, BLOCK_SIZE_K // 16)\n+    else:\n+        scale_offs = offs_k\n+\n+    b_scale_ptrs = (\n+        b_scale_ptr\n+        + off_experts * stride_bse\n+        + scale_offs[:, None] * stride_bsk\n+        + offs_bn[None, :]",
 "ttft_seconds": 0.3119024710031226,
 "status": "ok",
 "tg_tokens_per_s": 37.575553624343115,
 "decode_seconds_per_token": 0.02661304767449003,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 255,
  "median": 26.23125701211393,
  "p95": 33.385454007657245,
  "mean": 26.61304767449003
 },
 "class": "code",
 "requested_input_tokens": 2048
}
